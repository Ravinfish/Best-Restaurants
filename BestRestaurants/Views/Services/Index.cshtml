@{
  Layout = "_Layout";
}
@using BestRestaurants.Models;
<h1>Meal Services</h1>

@if (@Model.Count == 0)
{
  <h3>No meal services have been added yet...</h3>
}
@foreach (Service service in Model)
{
  <h5>@Html.ActionLink($"{service.Type}", "Details", new { id = service.ServiceId })</h5>
}
