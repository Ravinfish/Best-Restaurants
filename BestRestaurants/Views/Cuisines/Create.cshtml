@{
  Layout = "_Layout";
}

@model BestRestaurants.Models.Cuisine

<h4>Add a cuisine style</h4>
@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  <br />
  <input type="submit" value="Add new cuisine" />
}