@{
  Layout = "_Layout";
}

@using BestRestaurants.Models;
<h1>Days</h1>

@if (@Model.Count == 0)
{
  <h3> No days have been added yet... </h3>
}

@foreach (Day day in Model)
{
  <h5>@Html.ActionLink($"{day.Name}", "Details", new { id = day.DayId })</h5>
}
